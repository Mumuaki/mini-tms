# Analysis Report: TMSmini Gemini-v1

**Дата**: 2025-12-16
**Статус**: Готов к реализации (с замечаниями)

## 🔍 Состояние документации и кода

### 1. Спецификация (`SPECIFICATION.md`)
- **Статус**: Актуализирована пользователем.
- **Основные изменения**:
  - Добавлена возможность ручного ввода "Откуда".
  - Уточнены габариты для тестов (4.8 x 2.4 x 2.2).
  - Добавлено требование фильтрации по **весу (max payload)**.

### 2. План и Чек-лист (`PLAN.md`, `CHECKLIST.md`)
- **Статус**: В целом согласованы, но есть пробел в модели данных.
- **Проблема**: В чек-листе (CHK-BE-007) указана фильтрация по **весу**, но в `PLAN.md` и текущем `models.py` отсутствует поле `max_payload` для модели `Truck`.

### 3. Текущий Код (`models.py`)
- **Модель Truck**:
  - Есть: `cargo_length`, `cargo_width`, `cargo_height`.
  - **Отсутствует**: `max_payload` (Грузоподъемность).
- **Модель Freight**:
  - Есть поле `capacity` (строка), которое нужно будет парсить для сравнения.

## ⚠️ Выявленные несоответствия

1. **Поле Max Payload**: Для реализации требования "скрапер проверяет вес груза" необходимо добавить поле `max_payload` (Float) в модель `Truck`.
2. **Парсинг веса**: Данные о весе с Trans.eu приходят в строковом формате (напр., "1.5 t" или "24000 kg"). Потребуется утилита для нормализации этих данных.

## 🛠 Рекомендации перед генерацией задач

1. **Добавить миграцию**: Включить в Phase 1 задачу по добавлению `max_payload` в таблицу `trucks`.
2. **Утилита парсинга**: Запланировать создание функции `parse_weight_to_kg` в `trans_eu.py`.

---

**Готовность к генерации задач (/speckit.tasks):** ВЫСОКАЯ (после учета правки модели).
