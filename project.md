# Анализ фронтенд кодовой базы: TMSmini / Gemini

## 📁 Структура проекта

Кодовая база фронтенда расположена в директории `frontend` и имеет следующую структуру:

```
frontend/
├── dist/                   # Сборка для продакшена
├── public/                 # Статические ресурсы (favicon и т.д.)
├── src/                    # Исходный код приложения
│   ├── assets/             # Изображения, шрифты и другие ассеты
│   ├── components/         # React компоненты
│   │   ├── dashboard/      # Компоненты специфичные для дашборда
│   │   ├── layout/         # Компоненты разметки (Header, Footer, Sidebar)
│   │   └── trucks/         # Компоненты управления грузовиками
│   ├── pages/              # Компоненты-страницы (маршруты)
│   ├── services/           # Сервисы для внешних API (api.js)
│   ├── App.jsx             # Корневой компонент с роутингом
│   ├── main.jsx            # Точка входа в приложение
│   └── index.css           # Глобальные стили и Tailwind директивы
├── package.json            # Зависимости и скрипты
├── vite.config.js          # Конфигурация сборщика Vite
├── tailwind.config.js      # Конфигурация Tailwind CSS
└── eslint.config.js        # Конфигурация линтера ESLint
```

**Принципы организации:**
Проект организован по **функциональному признаку (Feature-based)** внутри директории `components` (папки `dashboard`, `trucks`), в сочетании с разделением по слоям (`components`, `pages`, `services`). Это упрощает навигацию и масшабирование.

## 🛠 Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 19.2.0 | Основная UI библиотека |
| **Vite** | 7.2.4 | Сборщик и Dev Server (быстрая сборка) |
| **React Router** | 7.9.6 | Клиентский роутинг |
| **Tailwind CSS** | 3.4.17 | CSS-фреймворк (Utility-first) |
| **Axios** | 1.13.2 | HTTP-клиент для API запросов |
| **Lucide React** | 0.555.0 | Библиотека иконок |
| **ESLint** | 9.39.1 | Линтинг кода |

## 🏗 Архитектура

### Компонентная архитектура
Используется современный подход на функциональных компонентах с хуками. Компоненты разделены на:
- **Pages**: Крупные компоненты, отвечающие за маршрут `(DashboardPage, FreightList)`.
- **Layouts**: Обертки для структуры страницы `(DashboardLayout, Layout)`.
- **UI Components**: Переиспользуемые части `(Button, Card - пока явно не выделены в common папку, но подразумеваются)`.

### Управление состоянием
На данном этапе используется **локальный стейт (useState/useReducer)**. Глобальный стейт менеджер (Redux/Zustand) отсутствует, что оправдано для "Mini-TMS". Данные передаются через пропсы или загружаются напрямую в страницах.

### API слой
Взаимодействие с бэкендом централизовано в `src/services/api.js`.
- Создан инстанс Axios с базовым URL `/api`.
- API методы сгруппированы в объекты-сервисы: `freightService`, `truckService`.
- Пример:
```javascript
export const freightService = {
    getAll: async (skip = 0, limit = 100) => {
        const response = await api.get('/freights', { params: { skip, limit } });
        return response.data;
    }
}
```

### Роутинг
Определен в `App.jsx`. Обнаружена потенциальная проблема несоответствия навигации:
- В `App.jsx` определены маршруты `/` и `/trucks`.
- В компоненте `Layout` есть ссылки на `/freights` и `/settings`, которые пока не подключены в `App.jsx` (хотя компонент `FreightList.jsx` существует).

## 🎨 UI/UX и стилизация

- **Tailwind CSS**: Используется для всех стилей. Стандартный подход Utility-first.
- **clsx / tailwind-merge**: Используется утилита `clsx` для условного рендеринга классов, что позволяет делать компоненты гибкими.
- **Иконки**: `lucide-react` обеспечивает единый стиль иконок.
- **Адаптивность**: В верстке замечены классы `md:`, `lg:`, что говорит о поддержке мобильных устройств (хотя таблица грузов может требовать доработки под мобайл).

## ✅ Качество кода

- **Линтинг**: Настроен ESLint 9 с плагинами для React и Hooks.
- **Типизация**: TypeScript **не используется**, проект на чистом JavaScript (ESM).
- **Тесты**: Тесты (Unit/E2E) в структуре проекта **отсутствуют**.
- **Чистота кода**: Код написан аккуратно, функции небольшие. Используются async/await. Обработка ошибок присутствует (try/catch в методах компонентов).

## 🔧 Ключевые компоненты

### 1. FreightList (`src/pages/FreightList.jsx`)
Основной экран для поиска грузов. Реализует сложную логику:
- Фильтрация (откуда, куда, даты).
- Запуск скрапера (взаимодействие с бэкендом).
- Отображение таблицы результатов.
- Таймер автообновления после запуска скрапинга.

```javascript
// Пример логики запуска скрапера
const handleScrape = async () => {
    try {
        setScraping(true);
        await freightService.scrape(filters.origin, ...);
        // Auto-refresh logic
        setTimeout(() => { fetchFreights(); setScraping(false); }, 10000);
    } catch (err) { ... }
};
```

### 2. Layout (`src/components/Layout.jsx` / `DashboardLayout`)
Отвечает за каркас приложения (Sidebar + Main Content).
Использует `react-router-dom` `Link` и `useLocation` для подсветки активного пункта меню.

### 3. API Service (`src/services/api.js`)
Абстракция над сетевыми запросами. Позволяет легко менять базовый URL или добавлять заголовки авторизации в будущем (interceptors).

## 📋 Выводы и рекомендации

**Сильные стороны:**
- Современный и быстрый стек (Vite + React 19).
- Чистая структура проекта и понятное разделение ответственности.
- Использование Tailwind CSS ускоряет разработку UI.

**Потенциальные области для улучшения (To-Do):**
1. **Роутинг**: Исправить `App.jsx`, добавив маршрут для `/freights` (компонент `FreightList`), чтобы навигация работала корректно.
2. **TypeScript**: Для проекта уровня TMS (много данных, DTO) крайне рекомендуется переход на TypeScript для надежности.
3. **Обработка форм**: В `FreightList` много инпутов управляется вручную через стейт. Стоит рассмотреть `react-hook-form` при усложнении форм.
4. **Тесты**: Добавить хотя бы минимальные Unit-тесты для утилит и API сервисов.
5. **Консистентность Layout**: Разобраться с дублированием `Layout.jsx` и `components/layout/DashboardLayout.jsx`.

**Уровень сложности**: Junior/Middle friendly. Код понятен, порог входа низкий.
